package game.id;

public enum Weapons {
	/*
	 * enum for each weapon type
	 * used for determining what
	 * to do with the current weapon
	 * equipped.
	 * (fist only used for no weapon)
	 */
	Fist(),
	PaintBrush(),
	Pistol(),
	Shotgun(),
	Rifle();
}
