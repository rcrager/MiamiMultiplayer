package game.id;

public enum ObjectID {
	/*
	 * enum for all object id's
	 * used for layering
	 */
	Player(),
	Wall(),
	Weapon(),
	Bullet(),
	LevelLoader(), 
	Spawn(), 
	Input(),
	Splatter();
}